package com.yuanyang.xiaohu.greendaodemo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.yuanyang.xiaohu.door.bean.CardRecord;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CARD_RECORD".
*/
public class CardRecordDao extends AbstractDao<CardRecord, Long> {

    public static final String TABLENAME = "CARD_RECORD";

    /**
     * Properties of entity CardRecord.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property No = new Property(1, String.class, "no", false, "NO");
        public final static Property DirectionDoor = new Property(2, String.class, "directionDoor", false, "DIRECTION_DOOR");
        public final static Property Accessible = new Property(3, String.class, "accessible", false, "ACCESSIBLE");
    }


    public CardRecordDao(DaoConfig config) {
        super(config);
    }
    
    public CardRecordDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CARD_RECORD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NO\" TEXT," + // 1: no
                "\"DIRECTION_DOOR\" TEXT," + // 2: directionDoor
                "\"ACCESSIBLE\" TEXT);"); // 3: accessible
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CARD_RECORD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CardRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String no = entity.getNo();
        if (no != null) {
            stmt.bindString(2, no);
        }
 
        String directionDoor = entity.getDirectionDoor();
        if (directionDoor != null) {
            stmt.bindString(3, directionDoor);
        }
 
        String accessible = entity.getAccessible();
        if (accessible != null) {
            stmt.bindString(4, accessible);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CardRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String no = entity.getNo();
        if (no != null) {
            stmt.bindString(2, no);
        }
 
        String directionDoor = entity.getDirectionDoor();
        if (directionDoor != null) {
            stmt.bindString(3, directionDoor);
        }
 
        String accessible = entity.getAccessible();
        if (accessible != null) {
            stmt.bindString(4, accessible);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CardRecord readEntity(Cursor cursor, int offset) {
        CardRecord entity = new CardRecord( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // no
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // directionDoor
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // accessible
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CardRecord entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNo(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDirectionDoor(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setAccessible(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CardRecord entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CardRecord entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CardRecord entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
